# secrets
.env

# python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# node / expo
node_modules/
.expo/
*.log

# ponytail is a separate checkout, not our source
tooling/ponytail/

# runtime data (the ledger is generated, never committed)
data/
*.jsonl
!fixtures/**/*.jsonl
.cache/

# expo prebuild output (regenerated; never hand-edit)
apps/mobile/android/
apps/mobile/ios/
apps/mobile/.expo/

# signing secrets: keystore + passwords. NEVER commit. Back them up separately.
apps/mobile/credentials/
