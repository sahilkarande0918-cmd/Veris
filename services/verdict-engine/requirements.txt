fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-dotenv>=1.0
httpx>=0.28

# deterministic checks
confusables>=1.2      # Unicode UTS #39 confusable skeleton (never hand-roll this table)
tldextract>=5.3       # Public Suffix List, pinned to its bundled offline snapshot
pyaxmlparser>=0.3     # read an APK manifest offline, without installing the app
python-multipart>=0.0.9  # APK upload on /check/apk

# test-only
pytest>=8.3
